version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - ${PORT}:8080
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production